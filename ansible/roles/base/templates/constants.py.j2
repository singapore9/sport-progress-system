ALLOWED_HOSTS = [
    "{{ constants_allowed_hosts|join('\",\n    \"') }}"
]

FIREBASE_CERTIFICATE = {{ constants_firebase_certificate|to_nice_json }}
DATABASE_URL = "{{ constants_firebase_database_url }}"